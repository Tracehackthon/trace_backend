# Trace 底层后端、模板与预设能力审查（更新至 2026-09-10）

## 结论

`trace-runtime@0.7.1` 已经达到 **Codex-first 底层基线可用**：协议、数据底座、SQLite、变化流程、连续性回执、Activation Pack、候选前例、能力发布、MyWiKi 正式源、知乎公开 transport、Codex hooks、doctor/backup/restore 都能从同一 TypeScript runtime 运行，旧 Python runtime 不再进入 active build。当前还具备第一层宿主来源证据评估：可回放真实 Codex cwd hook 的 source lease、native search/read evidence、读取预算与 privacy boundary，并生成 baseline/Trace 成对清单；另有可查看、可锁定的冷启动协作模型与认知源地图，不以隐式人格画像替代用户配置。新 runtime 与已有项目之间的差异由只读 `trace upgrade` 显示；它不把仓库更新误当成授权覆盖用户项目。

但它还不是“以后只做效果测试和功能迭代”的完整生产后端。SQLite 热路径、5 秒 busy timeout、四进程并发写入回归、Node 22 纯 JavaScript `sql.js`（asm build）fallback、Codex activation 的 privacy-safe correlation trace、主要 public contract 的只读 upcaster（Data 为 `0.1.0 → 0.2.0 → 0.3.0`，其余主要协议为 `0.1.0 → 0.2.0`），以及用户选择型 prompt case capture 已完成；跨机发布、Skill 目标选择、模板升级三方合并、DeepSeek Harness 的真实 adapter 和桌面 shell 仍是后续可靠性门禁。这些是边界和可靠性缺口，不是再重写数据底座。

## 审查证据

| 检查 | 结果 | 证据 |
|---|---|---|
| TypeScript 类型检查 | 通过 | `corepack pnpm typecheck` |
| TypeScript/SQLite/连接器/宿主/效果测试 | 通过，67 项 | `corepack pnpm test`；包含项目实例初始化、模板引用资源检查、SQLite fallback selection/实际打开、SQLite 热路径、四进程并发写入、correlation privacy、hook 原始 prompt 不落库、双项目 event-cwd 路由、external profile、host-native source lease、PreToolUse 读取预算、PostToolUse search/read evidence、12 条 fixture evidence replay、baseline/Trace manifest、prompt explicit capture、protocol upcast、release-plan 审计、source export 备份轮转、产品 CLI help 与 init → inbox → review → Codex hook → backup 路径、旧项目 profile lock 迁移与只读 runtime update inspection、deterministic validator fuzz corpus |
| Python 兼容 SDK 回归 | 通过，3 项 | `corepack pnpm test:python`；导出仓库只保留薄 SDK 测试，不是旧 runtime |
| 全部门禁 | 通过 | `corepack pnpm check:all` |
| 发布边界审计 | 可审阅 | `corepack pnpm audit:release-plan`；根 private runtime 不由 Changesets 自动升级，profile/bundle/runtime 必须先显式共同决策 |
| 发行包 | 通过 | `corepack pnpm package` 生成 `release-manifest.json`，条目数以当次 manifest 为准 |
| native 安装预检 | 通过 | `node native/install.mjs --dry-run` |
| 用户 SQLite | 按实例检查 | 使用目标项目 `.trace/state/trace.sqlite` 执行 `doctor run`，不依赖固定用户路径 |
| 知乎公开接口 | 已实测 | 故事列表 20 条、详情 1 条并写入临时 source snapshot；认证接口等待 `ZHIHU_ACCESS_SECRET` |

## 四类内容审查

### 1. 预设模板

- `trace.codex-starter`：Codex + 用户选择的 MyWiKi profile；预设的是结构、可见性、回执、治理和通用协作起点（先接住未成形思考、避免默认问卷、明确执行即执行、证据/沉淀分层），不复制任何用户历史或个人语义。
- `trace.codex-empty`：没有认知源和能力，只有隔离的渐进式披露治理；适合作为无来源冷启动。
- `trace.codex-team`：团队/项目作用域，必须显式选择团队认知源；本轮已补齐它声明的 capability/source/context 文件。
- 模板安装产生 `template.manifest.json`、`trace.lock.json` 和可选 `source.profile.json`；源 profile hash 与 scope 写入 lock，root 和凭证不写入模板。
- `project init` 会在项目下创建 `.trace/`：本地认知源、状态库、候选、回执和备份彼此分开；外部源只通过 profile 连接。

模板仍然是只读冷启动输入，不是用户认知源，也不是自动安装 Skill。模板升级必须 preview、三方 diff、Change Set、验证后才激活。

### 2. 预设给 Agent 的上下文

发行包只预设以下宿主可理解的治理上下文：渐进式披露、预算、禁止范围、激活/沉淀回执、proposal → adopt → publish、安全写入边界，以及通用冷启动协作模型。Codex hook 会按当前事件 cwd 自动加载对应项目 profile，编译版本化协作模型/认知源地图，再只把 formal source lease、预算和隐私规则返回给当前 Codex；Codex 自己检索/读取。`host_retrieval_evidence` 只保存实际访问的相对 locator、revision/hash 与事件 hash，不保留绝对路径、prompt、正文或工具参数。

用户的“思考优先级、Discuss-First、个人协作方式”等语义可由用户选择的 MyWiKi 正式页和本地协作模型/来源地图显式提供，不应硬编码成公共用户画像；这保证不同用户可以选择不同 cognitive source。`trace profile` 让用户看见当前模型、地图和 lock；`trace profile update` 是唯一的产品级更新入口，负责验证、备份与 hash lock 更新。早于该 lock 的项目会显示 `legacy_unlocked`，仅能经 `trace profile migrate --confirm true` 将正在使用的兼容配置固化，且不改 SQLite、来源、模板、能力或 hooks。当前 profile 默认 `write_enabled=false`，正式页写入只能走 proposal + revision/hash CAS + approval + backup + atomic rename。

### 3. 预设能力

当前模板能力是“Agent 协作治理”示例，包含 triggers、inputs、outputs、acceptance 和作用域；它的状态是 `template-preview`，不是已发布 Skill。真正发布仍须有采用的 `capability_candidate`、行为证据、能力内容契约、逐文件 hash 和显式 approval。

现有个人 Skill 的语义不被模板覆盖。Skill installer 已实现，但没有指定目标 Skill 时不会静默替换用户已有能力；这是正确的产品边界。

### 4. 外部连接与宿主

- 知乎：公开黑客松接口 transport 已真实回放；Access Secret 从环境变量读取且不进入错误信息。平台搜索/本人数据等需真实 secret 后再验收。
- MyWiKi：只读 formal `wiki/`，raw 不可写；正式页修改可审计、可回滚、拒绝 stale CAS。
- Codex：用户级 `hooks.json` 已切换到稳定 TS launcher，旧 Python hook 不再是入口；新 hook 按每次 event cwd 路由，不会让后启用项目覆盖另一项目的 profile/state。Skill/hook 都保留备份和 rollback receipt。每次 activation 与实际 host source access 都会保存不含 prompt/source body/absolute source path/tool args 的 correlation event 与 evidence，`doctor --correlation-id` 可查询其 refs、耗时和错误码。
- DeepSeek Harness：只有 plugin contract、profile 和 adapter 目录位置，未实现实际插件发现/事件映射。
- Desktop：只有 SDK/RPC 边界与视图约定，未构建 shell 或安装器。

## 后续判断

可以把“重写底层数据模型”暂时结束，进入 **效果验证 + 版本治理 + 有界功能迭代**；但每次迭代仍须先过下面四个门：

1. `check:all` 和模板资源审计；
2. 一条真实或隔离的 capture → activation → discussion → candidate → adoption → publish → rollback 回放；
3. 发行包 manifest 校验、干净目录安装和 doctor；
4. 记录 runtime/protocol/template/source/capability 各自版本，不把一次效果好误当作全局采用。

在 `ZHIHU_ACCESS_SECRET` 配置前，不把“知乎真实 transport 已完成”扩大解释为“所有知乎业务接口已验收”；在指定目标 Skill 并确认语义等价前，不执行用户级 Skill 替换；在 DeepSeek/Desktop 有真实宿主协议前，不把预留目录标为完成。

仓库更新后的用户项目遵循“检测而非覆盖”：新用户 `trace init` 使用新模板/starter；已 lock 项目保留自己的 profile、source、SQLite、模板、能力与 hooks；旧项目先以兼容 starter 运行并由 `trace profile migrate` 固化。模板三方合并仍未实现，因此已有模板没有自动升级路径；数据物理迁移、profile 更新、hooks 与 Skill 更新都必须分别预览/备份/确认。

## 效果评估的准确边界

`tests/evals/` 中的 12 条 synthetic golden case 现在是**第一层回归门禁**：它验证授权 MyWiKi formal page 的确定性检索、`activation_excluded_paths`、两条 pointer 上限、相对 durable locator、双项目 cwd 路由，以及 no-source-snapshot 的持久化边界。`corepack pnpm eval:pair` 会产生 baseline 和 Trace 的并列 `eval-manifest.json` 与 `pair-manifest.json`，且清单不含 raw prompt、来源正文、绝对来源路径或凭证。

这不是“模型效果已经验收”。Codex 通用 hook 目前没有“某页面实际被读取”的宿主事件，故 pointer 已提供不能证明 Agent 已阅读；Layer 2 的模型/用户效果评估仍需固定模型与工具权限、记录真实读页证据、最终 artifact、用户纠正次数、完成 turns、延迟及 scope/adoption 判断。发布时必须清楚标注这一层仍待真实宿主 trace 或人工判定，不得把 golden retrieval 分数扩大表述为通用智能体能力提升。

当前 SQLite 验收覆盖四个独立 Node writer 与一个共享 identity 竞争场景；它不等于桌面端、多用户共享目录或网络文件系统的容量承诺。协议升级均只创建内存视图、不会改写历史 revision；未知版本继续 fail-closed。Node 22–24.1 的发行包用纯 JavaScript `sql.js`（asm build）规避 `node:sqlite` experimental loading（带受控本地文件锁、写前重载和原子提交），Node >=24.2 使用稳定内置 driver；driver 选择可由环境变量和 `doctor` 观察。产品 CLI 默认只展示用户动作，旧协议命令仍通过 `trace internal` / `--help --advanced` 保持兼容；Desktop/DeepSeek 仍无真实宿主协议，因此明确保留而未实现。
