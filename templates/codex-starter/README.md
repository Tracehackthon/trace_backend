# Trace Codex Starter Template

这是一个可审查的冷启动模板，不会自动上传本地认知源、写入 Wiki 或发布能力。

包含：

- `agent-collaboration-governance@1.0.0`：预设能力引用；
- `trace-collaboration-starter@1.0.0`：认知源路由和读取范围；
- `trace-default-governance@1.0.0`：沉淀回执、激活回执和 Proposal → Adopt → Commit 规则。

安装后生成用户实例和锁定文件。用户修改写入 overlay，不修改模板本身。
