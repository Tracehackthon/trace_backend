# Trace 底层后端、模板与预设能力审查（2026-09-09）

## 结论

`trace-runtime@0.6.0` 已经达到 **Codex-first 底层基线可用**：协议、数据底座、SQLite、变化流程、连续性回执、Activation Pack、候选前例、能力发布、MyWiKi 正式源、知乎公开 transport、Codex hooks、doctor/backup/restore 都能从同一 TypeScript runtime 运行，旧 Python runtime 不再进入 active build。

但它还不是“以后只做效果测试和功能迭代”的完整生产后端。后续仍需先完成宿主与运行可靠性门禁：认证知乎业务接口、并发写入压力、发布包跨机器安装、Skill 目标选择、模板升级三方合并、DeepSeek Harness adapter 和桌面 shell。这些是边界和可靠性缺口，不是再重写数据底座。

## 审查证据

| 检查 | 结果 | 证据 |
|---|---|---|
| TypeScript 类型检查 | 通过 | `corepack pnpm typecheck` |
| TypeScript/SQLite/连接器/宿主测试 | 通过，32 项 | `corepack pnpm test`；包含项目实例初始化与模板引用资源检查 |
| Python 兼容 SDK/既有能力回归 | 通过，96 项 | `corepack pnpm test:python`；只保留薄 SDK，不是旧 runtime |
| 全部门禁 | 通过 | `corepack pnpm check:all` |
| 发行包 | 通过 | `corepack pnpm package` 生成 `release-manifest.json`，条目数以当次 manifest 为准 |
| native 安装预检 | 通过 | `node native/install.mjs --dry-run` |
| 用户 SQLite | 按实例检查 | 使用目标项目 `.trace/state/trace.sqlite` 执行 `doctor run`，不依赖固定用户路径 |
| 知乎公开接口 | 已实测 | 故事列表 20 条、详情 1 条并写入临时 source snapshot；认证接口等待 `ZHIHU_ACCESS_SECRET` |

## 四类内容审查

### 1. 预设模板

- `trace.codex-starter`：Codex + 用户选择的 MyWiKi profile；预设的是结构、可见性、回执和治理，不复制用户语义。
- `trace.codex-empty`：没有认知源和能力，只有隔离的渐进式披露治理；适合作为无来源冷启动。
- `trace.codex-team`：团队/项目作用域，必须显式选择团队认知源；本轮已补齐它声明的 capability/source/context 文件。
- 模板安装产生 `template.manifest.json`、`trace.lock.json` 和可选 `source.profile.json`；源 profile hash 与 scope 写入 lock，root 和凭证不写入模板。
- `project init` 会在项目下创建 `.trace/`：本地认知源、状态库、候选、回执和备份彼此分开；外部源只通过 profile 连接。

模板仍然是只读冷启动输入，不是用户认知源，也不是自动安装 Skill。模板升级必须 preview、三方 diff、Change Set、验证后才激活。

### 2. 预设给 Agent 的上下文

发行包只预设以下宿主可理解的治理上下文：渐进式披露、预算、禁止范围、激活/沉淀回执、proposal → adopt → publish、安全写入边界。Codex hook 只返回页面元数据和读取指针，不把 MyWiKi 正文或完整聊天塞进 prompt。

用户的“思考优先级、Discuss-First、个人协作方式”等语义仍来自用户选择的 MyWiKi 正式页，不应硬编码进公共模板；这保证不同用户可以选择不同 cognitive source。当前 profile 默认 `write_enabled=false`，正式页写入只能走 proposal + revision/hash CAS + approval + backup + atomic rename。

### 3. 预设能力

当前模板能力是“Agent 协作治理”示例，包含 triggers、inputs、outputs、acceptance 和作用域；它的状态是 `template-preview`，不是已发布 Skill。真正发布仍须有采用的 `capability_candidate`、行为证据、能力内容契约、逐文件 hash 和显式 approval。

现有个人 Skill 的语义不被模板覆盖。Skill installer 已实现，但没有指定目标 Skill 时不会静默替换用户已有能力；这是正确的产品边界。

### 4. 外部连接与宿主

- 知乎：公开黑客松接口 transport 已真实回放；Access Secret 从环境变量读取且不进入错误信息。平台搜索/本人数据等需真实 secret 后再验收。
- MyWiKi：只读 formal `wiki/`，raw 不可写；正式页修改可审计、可回滚、拒绝 stale CAS。
- Codex：当前用户 `hooks.json` 已切换到稳定 TS launcher，旧 Python hook 不再是入口；Skill/hook 都保留备份和 rollback receipt。
- DeepSeek Harness：只有 plugin contract、profile 和 adapter 目录位置，未实现实际插件发现/事件映射。
- Desktop：只有 SDK/RPC 边界与视图约定，未构建 shell 或安装器。

## 后续判断

可以把“重写底层数据模型”暂时结束，进入 **效果验证 + 版本治理 + 有界功能迭代**；但每次迭代仍须先过下面四个门：

1. `check:all` 和模板资源审计；
2. 一条真实或隔离的 capture → activation → discussion → candidate → adoption → publish → rollback 回放；
3. 发行包 manifest 校验、干净目录安装和 doctor；
4. 记录 runtime/protocol/template/source/capability 各自版本，不把一次效果好误当作全局采用。

在 `ZHIHU_ACCESS_SECRET` 配置前，不把“知乎真实 transport 已完成”扩大解释为“所有知乎业务接口已验收”；在指定目标 Skill 并确认语义等价前，不执行用户级 Skill 替换；在 DeepSeek/Desktop 有真实宿主协议前，不把预留目录标为完成。
